# Datasets

No external dataset files are required to reproduce the experiments.

- `exp07_real_data.py` uses the standard datasets bundled with scikit-learn
  (`iris`, `wine`, `breast_cancer`, `digits`); no downloads needed.
- All other experiments generate synthetic data internally with fixed seeds.
