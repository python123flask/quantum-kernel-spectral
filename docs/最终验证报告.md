# 最终验证报告（2026-09-07，投稿前）

> 目的：科学正确性的四层独立验证，作为投稿透明性记录。

## 1. 物理正确性（独立解析参照）

| 验证项 | 方法 | 结果 |
|--------|------|------|
| 单 qubit 角度编码核 | vs 解析式 k(x,y)=cos²((x−y)/2) | 偏差 2.2e-16（机器精度）✅ |
| 多 qubit 直积核 | vs 解析式 Πcos²((x_i−y_i)/2) | 偏差 4.4e-16 ✅ |
| 单比特门（H/X/R_z） | 单元测试 + 幺正性 | 全部通过 ✅ |
| 编码归一化 | 全部 4 特征映射 | 全部通过 ✅ |
| 保真度核正定/对称/对角1 | 数值检验 | 通过 ✅ |
| 振幅阻尼 Kraus 通道 | 保迹检验 tr=1.0 | 通过 ✅ |

> 历史上发现并修复两个真实编码 bug（R_z 门错误实现、IQP 缺 H 层），
> 修复后全部实验重跑、全部数字更新（见 results/实验结果备忘03-最终版.md）。

## 2. 统计正确性

| 验证项 | 方法 | 结果 |
|--------|------|------|
| BH-FDR 实现 | vs 独立手算（p=[.01,.03,.1,.4] 参照） | q 值逐项一致 ✅ |
| Spearman 相关 | scipy.stats.spearmanr 标准实现 | ✅ |
| 多重比较校正 | 全部核心检验报告 FDR q 值 | ✅ |

## 3. 数据一致性（论文 ↔ CSV）

全部关键统计量（exp01-11 的 ρ/p/q/均值/百分比）与原始 CSV 独立复算一致，
含：谱判据五项、单任务相关、跨任务全局、难度主导（全局+分层+任务级）、
TA 对齐（全局+残差）、exp10/11 全部新结果。

## 4. 可复现性

- 固定种子；重跑 exp01 与既有 CSV **md5 一致**（比特级相同）
- 22 个 Python 文件语法检查通过；6 核心模块导入通过；单元测试全过
- 依赖仅 numpy/scipy/matplotlib/scikit-learn/pandas
- 代码+数据+图全部公开：github.com/python123flask/quantum-kernel-spectral

## 5. 已如实披露的方法学细节（论文中）

- 任务-seed 块内映射变体共享数据（有效样本量说明）→ 论文 Statistics 节
- RFF 近似含抽样噪声（0.08-0.17 量级）→ 论文 Statistics 节
- AI 使用范围（代码/图脚本/语言编辑）与图表非 AI 生成 → Acknowledgements
- 数据声明与开源现状一致 → Data availability

## 结论

论文与代码在物理、统计、数据、复现四个层面均通过独立验证，可投稿。

---

## 附录：投稿后的两轮修正记录（2026-09-10）

### 修正 1：数据可用性声明
原声明误写“代码与数据作为 arXiv 附件随稿提供”（实际未发布 arXiv）。
已删除该表述，仅保留真实 GitHub 链接。

### 修正 2：exp04 相关系数口径（重要）
- 原写法：频率尺度→有效秩 “ρ=+1.000, p=1.4e-24”（5 个均值点口径）
- 问题：（a）口径未说明，读者拿 45 个配置复算得到 0.985；
  （b）**p=1.4e-24 是 scipy 渐近近似在 n=5 完全单调时的失效产物**
  （5 个点的 p 值理论下限为 0.017，不可能出现 1.4e-24）
- 已改为全配置口径：ρ=+0.985 (p=3.2e-34) / +0.884 (p=9.2e-16)；
  同步更新纠缠（+0.20→+0.093）与 IQP 深度（+0.90→+0.775）
- 结论方向完全不变

### 修正 3：脚本与公开数据一致性
发现内存计算与写出 CSV 在近并列值上有微差（0.1095 vs 0.0932）。
已将 exp04 脚本改为“写出后重新读回再做分析”，保证脚本输出、CSV、论文三者完全一致。

### 修正 4：配置总数 721→781
摘要漏计 exp10（24）与 exp11（36）：64+72+216+114+120+51+48+36+24+36 = 781。已更正。

### 修正 5：残差口径说明
“核-目标对齐残差归零”标注为 rank-space 残差（与脚本一致：ρ=+0.001, p=0.99）；
原始值口径为 +0.047（同样不显著），结论不变。

### 附：p 值独立核对（全部 12 组关键 p 值）
用 t 近似公式独立复算 + 置换检验交叉验证：
除已修正的 n=5 情形外，其余全部与论文报告值一致（n≥15），无其他同类问题。
